'use strict';

/**
 * user-tome-comment service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::user-tome-comment.user-tome-comment');
